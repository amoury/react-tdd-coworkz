const initialState = false;

const authReducer = (state = initialState, action) => {
  switch (action.type) {
    case "calue":
      return state;
  
    default:
      return state;
  }
}

export default authReducer;